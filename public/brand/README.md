Place the client-provided logo SVG here as `logo.svg` (tree + elderly couple silhouette).

The app will pick it up automatically once added. Until then, a sage-green placeholder slot is shown in the navigation.
